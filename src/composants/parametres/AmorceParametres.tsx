"use client";

/* Ne rend rien : importer parametres-demo dans la coquille suffit à charger les
 * libellés des documents dans le navigateur avant le premier rendu, pour que
 * les pages nomment les documents ajoutés par le métier comme le serveur. */
import "@/lib/parametres-demo";

export function AmorceParametres() {
  return null;
}
