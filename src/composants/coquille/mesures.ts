/**
 * Mesures partagées de la coquille.
 *
 * La barre d'application est collée en haut ; tout ce qui doit rester visible
 * sous elle — en-têtes de tableau notamment — s'y adosse à cette hauteur.
 */
export const HAUTEUR_BARRE = 60;

/** Largeur du rail de navigation, déployé puis rétracté en icônes. */
export const LARGEUR_RAIL = 240;
export const LARGEUR_RAIL_RETRACTE = 64;
